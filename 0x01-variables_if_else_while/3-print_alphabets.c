#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	char onah;
	char godwin;

	for (onah = 'a'; onah <= 'z'; onah++)
		putchar(c);
	for (godwin = 'A'; godwin <= 'Z'; godwin++)
		putchar(d);
	putchar('\n');
	return (0);
}
