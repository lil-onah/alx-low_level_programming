#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	char onah;

	for (onah = 'a'; onah <= 'z'; onah++)
	{
		putchar(onah);
	}
	putchar('\n');
	return (0);
}
