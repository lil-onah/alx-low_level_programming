#include "main.h"

/**
 * mul - prints multiple of two numbers
 * @a: first number that multiplies with another digit
 * @b: multiplies with first digits
 * Return: return Always (success)
 */

int mul(int a, int b)
{
	return (a * b);
}
